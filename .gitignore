# vagrant
.vagrant

# terraform
terraform/.*
terraform/vars/nonprod.tfvars
terraform/*.tfstate*
terraform/terraform.tfstate

# puppet
puppet/.librarian
puppet/.tmp
puppet/manifests-*
puppet/modules/*
puppet/modules-*
puppet/Puppetfile.lock
