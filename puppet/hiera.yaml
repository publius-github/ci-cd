---
version: 5
defaults:
  :data_hash: 'yaml_data'
  :datadir: 'hieradata'

hierarchy:
  - name: 'Instance role apache'
    path: "roles/%{::role}.yaml"

  #   path: "roles/%{::role}.yaml"

  - name: 'Common data to everything'
    path: 'common.yaml'
